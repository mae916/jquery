jQuery
  window.document 객체  - HTML 문서와 관련 기능
                        - HTML을 변경, 수정, 추가, 삭제
                        - 속성을 변경....

                        - 요소를 선택 -> HTML을 변경, 수정, 추가, 삭제
                        - 아이디 - getElementById
                        - 클래스 - getElementByClassName
                        - 태그명 - getElementByTagName
                        - Name속성 - getElementByName
                        - CSS 속성 - querySelector, querySelectorAll

  - 주로 선택자와 관련된 편의 기능 -> 선택자가 가장 중요

  jquery 라이브러리를 사용하는 방법
    1)소스를 다운로드 받아서 사용하는 방법(jquery 공식 사이트)
    2)CDN(contents delivery network) - URL 형태로 외부서버에서 제공받는 형태
    3)google cdn
      -google cdn jquery라고 검색 -> google Hosted Libraries 밑에 jquery라고 쓰여있는거 클릭
      -> 최신버전 복사해서 script src에 붙여넣기

  jQuery 생성자, 객체
    $ == jQuery
    jQuery로 쓰기보다는 $로 더 많이 쓴다.

    HTMl 태그를 선택할때
      DOMContentLoaded --> load
      (DOM트리 구축 완료)

      $(document).ready(function(){
      //DOMContentLoaded 발생 이후 , DOM트리 로드가 완료됐다는 뜻.
      });

      $(function() {
      //상기 방식과 동일
      });
